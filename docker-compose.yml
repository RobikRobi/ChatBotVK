services:
  vk-schedule-bot:
    build: .
    container_name: vk-schedule-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./outputs:/app/outputs:ro
